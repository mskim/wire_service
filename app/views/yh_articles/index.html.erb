<p id="notice"><%= notice %></p>

<h3 align="center">연합기사</h3>
<%= search_form_for @q do |f| %>
<%= f.label '카테고리에 포함된 글자로' %>
<%= f.select :category_cont,  options_for_select(%w[정치 경제 사회 문화 스포츠/레저 국제 지방 IT/과학 북한 민족]),  :include_blank => true%>
<%= f.label '제목에 포함된 글자로' %>
<%= f.search_field :title_cont %>
<%= f.submit %>
<% end %>
<br>

<table>
  <thead>
    <tr>
      <th>날짜</th>
      <th>시간</th>
      <th>카테고리</th>
      <th>제목</th>
      <th>등록기자</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @yh_articles.each do |yh_article| %>
       <tr>
        <td><%= yh_article.date %></td></a>
        <td><%= yh_article.time %></td>
        <td><%= yh_article.category %></td>
        <td><%= yh_article.title %></td>
        <td><%= yh_article.taken_by %></td>
        <td><%= link_to 'Show', yh_article %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Yh Article', new_yh_article_path %>

<div align="center">
  <%= paginate @yh_articles, :theme => 'twitter-bootstrap-3' %>
</div>